extends layout

block content
  h1= title
  p Welcome to UPLOAD GUY, a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    - let files = fileInfo.Contents
    div  
      each file in files.length ? files : ['NO!!!']
        audio(controls='', type='audio/mpeg')
          source(src='https://fk-audio.s3.amazonaws.com/'+file.Key)

      