extends layout

block content
  h1= title
  p Welcome to Flame Keeper

  h1 DASHBOARD

  if error
    p Error getting dynamic content.
  else
    p UPLOAD STUFF:
    form(method="POST" action="/upload" id="uploadForm" encType="multipart/form-data")      
      input(type="file" name="theUpload")
      input(type="submit" form="uploadForm" value="UPLOAD")
  br
  br

  if error
    p Error getting dynamic content.
  else
    p The library has the following files:

    - let returns = fileInfo.Contents.reverse();
    - let files = returns.slice(0,7);
        
    div
    each file in files ? files : ['NO!!!']      
      p= file.Key
      p= file.LastModified        
      form(method="GET" action="/download" id="dashboardForm" encType="multipart/form-data")                  
        input(type="checkbox" name="theDownload" checked="true" multiple)
    input(type="submit" form="dashboardForm" value="Submit")
          
      