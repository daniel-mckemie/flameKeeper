extends layout

block content
  h1= title
  p Welcome to Flame Keeper

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:
    - console.log(files.lastModified);

      
    audio(preload="auto", autobuffer, controls)  
      source(src="downloadedFile.Body" type="audio/mp3")

      